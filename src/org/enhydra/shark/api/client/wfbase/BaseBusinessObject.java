package org.enhydra.shark.api.client.wfbase;

/**
 * Java version of OMG's interface. This is how OMG defines it:
 * <p>
 * A BaseBusinessObject is the base interface for all business
 * object interfaces. It serves as a placeholder for the definition
 * of a business component base entity; it is assumed that a future
 * OMG initiative will provide a detailed specification of a base
 * business component interface which will replace the placeholder
 * used in this specification.
 */
public interface BaseBusinessObject
{
}
